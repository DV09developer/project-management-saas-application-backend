# this are the installed packages required in the application
npm install express
npm i mongoose
npm i bcrypt
npm i cookie-parser
npm i cors
npm i dotenv
npm i jsonwebtoken

# wait this will be install when needed
npm i mongoose-aggregate-paginate-v2